/**
 * Autogenerated by Avro
 * 
 * DO NOT EDIT DIRECTLY
 */

extension CLVModels.Base {

    public enum ChallengeType: Codable {
        case DUPLICATE_CHALLENGE
        case OFFLINE_CHALLENGE
        case PARSE_UNKNOWN(String)

        enum Key: CodingKey {
            case rawValue
        }

        public init(rawValue:String) {
            switch rawValue {
                case  "DUPLICATE_CHALLENGE":
                    self = .DUPLICATE_CHALLENGE
                case  "OFFLINE_CHALLENGE":
                    self = .OFFLINE_CHALLENGE
                default:
                    self = .PARSE_UNKNOWN(rawValue)
            }
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.singleValueContainer()
            let rawValue = try container.decode(String.self)
            self.init(rawValue: rawValue)
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.singleValueContainer()
            try container.encode(toString)
        }

        public var toString: String {
            switch self {
                case  .DUPLICATE_CHALLENGE:
                    return "DUPLICATE_CHALLENGE"
                case  .OFFLINE_CHALLENGE:
                    return "OFFLINE_CHALLENGE"
                case .PARSE_UNKNOWN(let rawValue):
                    return rawValue            
            }
        }
    }
}