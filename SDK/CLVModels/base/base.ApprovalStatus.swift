/**
 * Autogenerated by Avro
 * 
 * DO NOT EDIT DIRECTLY
 */

extension CLVModels.Base {

    public enum ApprovalStatus: Codable {
        case NEW
        case PENDING
        case APPROVED_PENDING_SIGNING
        case APPROVED
        case DENIED
        case PUBLISHED
        case PARSE_UNKNOWN(String)

        enum Key: CodingKey {
            case rawValue
        }

        public init(rawValue:String) {
            switch rawValue {
                case  "NEW":
                    self = .NEW
                case  "PENDING":
                    self = .PENDING
                case  "APPROVED_PENDING_SIGNING":
                    self = .APPROVED_PENDING_SIGNING
                case  "APPROVED":
                    self = .APPROVED
                case  "DENIED":
                    self = .DENIED
                case  "PUBLISHED":
                    self = .PUBLISHED
                default:
                    self = .PARSE_UNKNOWN(rawValue)
            }
        }

        public init(from decoder: Decoder) throws {
            let container = try decoder.singleValueContainer()
            let rawValue = try container.decode(String.self)
            self.init(rawValue: rawValue)
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.singleValueContainer()
            try container.encode(toString)
        }

        public var toString: String {
            switch self {
                case  .NEW:
                    return "NEW"
                case  .PENDING:
                    return "PENDING"
                case  .APPROVED_PENDING_SIGNING:
                    return "APPROVED_PENDING_SIGNING"
                case  .APPROVED:
                    return "APPROVED"
                case  .DENIED:
                    return "DENIED"
                case  .PUBLISHED:
                    return "PUBLISHED"
                case .PARSE_UNKNOWN(let rawValue):
                    return rawValue            
            }
        }
    }
}