/**
 * Autogenerated by Avro
 * 
 * DO NOT EDIT DIRECTLY
 */


import Foundation

extension CLVModels.Customers {

    public class EmailAddress: Codable {

        public var id: String?
        public var emailAddress: String?
        public var verifiedTime: Date?
        public var primaryEmail: Bool?
        /// Customer who this email address belongs to.
        public var customer: CLVModels.Base.Reference?

        public var decodeError:[Swift.Error] = [Swift.Error]()

        enum CodingKeys: CodingKey {
            case id
            case emailAddress
            case verifiedTime
            case primaryEmail
            case customer
        }

        public init() {}

        required public init(from decoder: Decoder) throws {
            let rootContainer = try decoder.container(keyedBy: CodingKeys.self)
            do {
                self.id = try rootContainer.decodeIfPresent(String.self, forKey: .id)
            } catch {
                decodeError.append(error)
            }
            do {
                self.emailAddress = try rootContainer.decodeIfPresent(String.self, forKey: .emailAddress)
            } catch {
                decodeError.append(error)
            }
            do {
                self.verifiedTime = try rootContainer.decodeIfPresent(Date.self, forKey: .verifiedTime)
            } catch {
                decodeError.append(error)
            }
            do {
                self.primaryEmail = try rootContainer.decodeIfPresent(Bool.self, forKey: .primaryEmail)
            } catch {
                decodeError.append(error)
            }
            do {
                self.customer = try rootContainer.decodeIfPresent(CLVModels.Base.Reference.self, forKey: .customer)
            } catch {
                decodeError.append(error)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            if (id != nil) { try container.encode(id, forKey: .id) }
            if (emailAddress != nil) { try container.encode(emailAddress, forKey: .emailAddress) }
            if (verifiedTime != nil) { try container.encode(verifiedTime, forKey: .verifiedTime) }
            if (primaryEmail != nil) { try container.encode(primaryEmail, forKey: .primaryEmail) }
            if (customer != nil) { try container.encode(customer, forKey: .customer) }
        }
    }

}