/**
 * Autogenerated by Avro
 * 
 * DO NOT EDIT DIRECTLY
 */


import Foundation

extension CLVModels.Resellers {

    public class ResellerFdmpConfiguration: Codable {

        public var id: String?
        public var userKey: String?
        public var unhashedSecret: String?

        public var decodeError:[Swift.Error] = [Swift.Error]()

        enum CodingKeys: CodingKey {
            case id
            case userKey
            case unhashedSecret
        }

        public init() {}

        required public init(from decoder: Decoder) throws {
            let rootContainer = try decoder.container(keyedBy: CodingKeys.self)
            do {
                self.id = try rootContainer.decodeIfPresent(String.self, forKey: .id)
            } catch {
                decodeError.append(error)
            }
            do {
                self.userKey = try rootContainer.decodeIfPresent(String.self, forKey: .userKey)
            } catch {
                decodeError.append(error)
            }
            do {
                self.unhashedSecret = try rootContainer.decodeIfPresent(String.self, forKey: .unhashedSecret)
            } catch {
                decodeError.append(error)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            if (id != nil) { try container.encode(id, forKey: .id) }
            if (userKey != nil) { try container.encode(userKey, forKey: .userKey) }
            if (unhashedSecret != nil) { try container.encode(unhashedSecret, forKey: .unhashedSecret) }
        }
    }

}