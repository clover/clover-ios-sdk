/**
 * Autogenerated by Avro
 * 
 * DO NOT EDIT DIRECTLY
 */


import Foundation

extension CLVModels.Account {

    public class SmsPromoRequest: Codable {

        /// Unique identifier
        public var phoneNumberId: String?
        /// The Operating System for the phone, used to decide what link to app store will be sent
        public var phoneOS: CLVModels.Account.PhoneOS?

        public var decodeError:[Swift.Error] = [Swift.Error]()

        enum CodingKeys: CodingKey {
            case phoneNumberId
            case phoneOS
        }

        public init() {}

        required public init(from decoder: Decoder) throws {
            let rootContainer = try decoder.container(keyedBy: CodingKeys.self)
            do {
                self.phoneNumberId = try rootContainer.decodeIfPresent(String.self, forKey: .phoneNumberId)
            } catch {
                decodeError.append(error)
            }
            do {
                self.phoneOS = try rootContainer.decodeIfPresent(CLVModels.Account.PhoneOS.self, forKey: .phoneOS)
            } catch {
                decodeError.append(error)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            if (phoneNumberId != nil) { try container.encode(phoneNumberId, forKey: .phoneNumberId) }
            if (phoneOS != nil) { try container.encode(phoneOS, forKey: .phoneOS) }
        }
    }

}