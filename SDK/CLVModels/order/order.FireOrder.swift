/**
 * Autogenerated by Avro
 * 
 * DO NOT EDIT DIRECTLY
 */


import Foundation

extension CLVModels.Order {

    public class FireOrder: Codable {

        /// Unique identifier
        public var id: String?
        /// The id of the order to fire.
        public var orderId: String?
        /// How long in seconds to wait before calling fire.
        public var delay: Int64?

        public var decodeError:[Swift.Error] = [Swift.Error]()

        enum CodingKeys: CodingKey {
            case id
            case orderId
            case delay
        }

        public init() {}

        required public init(from decoder: Decoder) throws {
            let rootContainer = try decoder.container(keyedBy: CodingKeys.self)
            do {
                self.id = try rootContainer.decodeIfPresent(String.self, forKey: .id)
            } catch {
                decodeError.append(error)
            }
            do {
                self.orderId = try rootContainer.decodeIfPresent(String.self, forKey: .orderId)
            } catch {
                decodeError.append(error)
            }
            do {
                self.delay = try rootContainer.decodeIfPresent(Int64.self, forKey: .delay)
            } catch {
                decodeError.append(error)
            }
        }

        public func encode(to encoder: Encoder) throws {
            var container = encoder.container(keyedBy: CodingKeys.self)
            if (id != nil) { try container.encode(id, forKey: .id) }
            if (orderId != nil) { try container.encode(orderId, forKey: .orderId) }
            if (delay != nil) { try container.encode(delay, forKey: .delay) }
        }
    }

}